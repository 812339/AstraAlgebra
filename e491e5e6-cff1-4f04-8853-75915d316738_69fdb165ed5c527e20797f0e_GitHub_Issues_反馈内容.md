## Issue 1: Bug 反馈

---

### 标题：[Bug] GPU.cpp 编译错误：缺少 `#include <cstring>`

**标签建议**：`bug`

---

### 正文内容（直接复制）：

```markdown
## Bug 描述

在 Linux 环境（GCC 11.4）下编译项目时，`src/Core/GPU.cpp` 文件编译失败，提示 `memcpy` 和 `memset` 未声明。

## 编译环境

- **操作系统**: Ubuntu 22.04 (Linux)
- **编译器**: GCC 11.4.0
- **CMake**: 3.22.1
- **编译选项**: `-DCMAKE_BUILD_TYPE=Release -DBUILD_TESTS=ON`

## 错误信息

```
/data/user/work/AstraAlgebra/src/Core/GPU.cpp: In static member function 'static void AstraAlgebra::GPUMemory::copyHostToDevice(void*, const void*, size_t)':
/data/user/work/AstraAlgebra/src/Core/GPU.cpp:203:5: error: 'memcpy' was not declared in this scope
  203 |     memcpy(devicePtr, hostPtr, size);
      |     ^~~~~~
/data/user/work/AstraAlgebra/src/Core/GPU.cpp:14:1: note: 'memcpy' is defined in header '<cstring>'; did you forget to '#include <cstring>'?
```

同样的错误出现在以下函数中：
- `GPUMemory::copyHostToDevice` (line 203)
- `GPUMemory::copyDeviceToHost` (line 214)
- `GPUMemory::copyDeviceToDevice` (line 225)
- `GPUMemory::memset` (line 236)

## 修复方法

在 `src/Core/GPU.cpp` 文件头部添加 `#include <cstring>`：

```cpp
#include <cstdlib>  // 包含std::malloc和std::free的定义
#include <cstring>  // 包含memcpy和memset的定义  // <-- 添加此行
```

## 复现步骤

1. 在 Linux 环境下克隆仓库
2. 执行 `mkdir build && cd build`
3. 执行 `cmake .. -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTS=ON`
4. 执行 `make`

## 附加说明

此问题仅在 Linux/GCC 环境下出现，Windows 平台的 MSVC 可能通过其他头文件间接引入了 `memcpy`，因此未暴露此问题。建议添加该头文件以确保跨平台兼容性。
```

---

## Issue 2: 性能对比报告（含方法论说明）

---

### 标题：[Report] AstraAlgebra vs GLM 实测性能对比报告（含 LTO 公平性验证）

**标签建议**：`performance`, `benchmark`

---

### 正文内容（直接复制）：

```markdown
## 概述

我对 AstraAlgebra 和主流游戏数学库 GLM 进行了实际编译运行的基准测试对比。为了确保公平性，我特别针对两个库的架构差异（Header-only vs 编译库）做了控制变量测试。

## ⚠️ 重要：测试方法论说明

GLM 是 **Header-only** 库，所有函数在编译时直接内联到调用点，天然获得了极大的优化优势。而 AstraAlgebra 是**编译库**（.a/.so），函数调用跨越翻译单元（Translation Unit），编译器默认无法内联。

为了排除这个架构差异带来的不公平因素，我进行了**两组测试**：
1. **常规测试**（-O3）：模拟用户默认使用场景
2. **LTO 测试**（-O3 -flto）：开启链接时优化，允许编译器跨翻译单元内联 AstraAlgebra 的函数

## 测试环境

| 项目 | 配置 |
|------|------|
| 编译器 | GCC 11.4 |
| 优化级别 | -O3 / -O3 -flto |
| SIMD 指令集 | AVX2 |
| 迭代次数 | 10,000,000 次 |
| 测试方法 | 动态随机数据，防止编译器过度优化 |
| GLM 版本 | 最新版 (GitHub master) |
| AstraAlgebra 版本 | 最新版 (GitHub main) |

## 测试结果

### Vector3 运算

| 运算类型 | Astra (常规) | Astra (LTO) | GLM | 常规结论 | LTO 结论 |
|---------|-------------|------------|-----|---------|---------|
| 加法 | 133.7 ms | 162.7 ms | 168.7 ms | Astra 快 7.7% | 相当 |
| 点积 | 135.2 ms | 161.1 ms | 174.9 ms | Astra 快 9.1% | Astra 快 8.6% |
| 叉积 | 139.5 ms | 166.5 ms | 177.6 ms | Astra 快 6.3% | Astra 快 6.7% |
| 归一化 | 246.4 ms | 266.9 ms | 109.8 ms | GLM 快 158% | GLM 快 143% |

### Matrix4x4 运算

| 运算类型 | Astra (常规) | Astra (LTO) | GLM | 常规结论 | LTO 结论 |
|---------|-------------|------------|-----|---------|---------|
| 乘法 | 296.5 ms | 370.1 ms | 338.1 ms | 相当 | GLM 快 9.4% |
| 求逆 | 1166.4 ms | 1173.5 ms | 437.1 ms | GLM 快 170% | GLM 快 168% |
| 变换向量 | 382.4 ms | 396.3 ms | 379.6 ms | GLM 快 9.7% | 相当 |

### Quaternion 运算

| 运算类型 | Astra (常规) | Astra (LTO) | GLM | 常规结论 | LTO 结论 |
|---------|-------------|------------|-----|---------|---------|
| 乘法 | 602.5 ms | 647.5 ms | 356.5 ms | GLM 快 77% | GLM 快 82% |
| 旋转向量 | 534.4 ms | 559.2 ms | 291.3 ms | GLM 快 98% | GLM 快 92% |
| Slerp | 1479.1 ms | 1484.0 ms | 793.2 ms | GLM 快 90% | GLM 快 87% |

### 实际场景

| 场景 | Astra (常规) | Astra (LTO) | GLM | 常规结论 | LTO 结论 |
|-----|-------------|------------|-----|---------|---------|
| lookAt | 957.9 ms | 989.5 ms | 282.6 ms | GLM 快 261% | GLM 快 250% |
| perspective | 1209.7 ms | 1200.7 ms | 134.1 ms | GLM 快 859% | GLM 快 795% |
| MVP 组合 | 3003.7 ms | 1546.1 ms | 533.7 ms | GLM 快 492% | GLM 快 190% |

## 关键发现：LTO 对 AstraAlgebra 几乎没有帮助

这是本次测试最重要的发现：

| 运算类型 | 常规耗时 | LTO 耗时 | 变化 |
|---------|---------|---------|------|
| Vector3 加法 | 133.7 ms | 162.7 ms | **反而变慢 21%** |
| Vector3 点积 | 135.2 ms | 161.1 ms | **反而变慢 19%** |
| Matrix4x4 求逆 | 1166.4 ms | 1173.5 ms | 几乎无变化 |
| Quaternion Slerp | 1479.1 ms | 1484.0 ms | 几乎无变化 |
| perspective | 1209.7 ms | 1200.7 ms | 几乎无变化 |
| MVP 组合 | 3003.7 ms | 1546.1 ms | **改善 48%** |

**结论：性能差距不是"内联"问题，而是算法实现本身的差异。**

LTO 开启后，AstraAlgebra 的函数已经被内联了（部分测试反而变慢说明编译器做了不同的优化决策），但性能差距依然存在。这说明 GLM 的优势来自：
1. **更高效的算法实现**（如归一化使用了 rsqrt 指令）
2. **更好的 SIMD 利用率**
3. **多年积累的底层优化**

唯一显著受益于 LTO 的是 MVP 组合（从慢 492% 改善到慢 190%），说明 lookAt 和 perspective 的跨函数调用确实有一定开销。

## AstraAlgebra 优势

- Vector3 基础运算（加法、点积、叉积）在常规模式下比 GLM 快 6-9%
- Matrix4x4 乘法、变换向量与 GLM 性能相当
- 功能更全面（碰撞检测、Transform 系统、GPU 加速等）

## GLM 优势

- Vector3 归一化快 143-158%
- Matrix4x4 求逆快 168-170%
- Quaternion 运算全面领先，快 77-98%
- 实际场景（lookAt、perspective、MVP）大幅领先

## 测试代码

如需要测试代码，我可以提供完整的统一基准测试程序。

## 总结

AstraAlgebra 在功能完整性方面有独特优势（碰撞检测、Transform 系统、GPU 加速），但在纯数学运算性能方面与 GLM 还有差距。**这个差距主要来自算法实现层面，而非库的架构形式（Header-only vs 编译库）。**

建议关注性能优化，特别是在以下方面：
- Vector3 归一化（可参考 GLM 的 rsqrt 实现）
- Quaternion 运算（乘法、旋转向量、Slerp）
- lookAt / perspective 函数

感谢作者开发这个库，期待后续优化！
```

---

## Issue 3: 性能优化建议（已更新）

---

### 标题：[Suggestion] 性能优化建议 - 基于 GLM 对比测试（含 LTO 验证）

**标签建议**：`enhancement`, `performance`

---

### 正文内容（直接复制）：

```markdown
## 背景

我进行了 AstraAlgebra 与 GLM 的实测性能对比，并特别做了 LTO（链接时优化）公平性验证。详见性能对比报告 Issue。

**核心发现**：性能差距不是库架构（Header-only vs 编译库）造成的，而是算法实现本身的差异。开启 LTO 后 AstraAlgebra 性能几乎没有改善，说明瓶颈在算法层面。

## 优化建议

### 1. Vector3 归一化性能优化（优先级：高）

**问题**：归一化比 GLM 慢 143-158%，且 LTO 无帮助

**分析**：GLM 的归一化很可能使用了 SIMD `rsqrt`（快速平方根倒数）指令，而 AstraAlgebra 可能使用了标准的 `sqrt` + 除法。

**建议**：
- 检查 `normalized()` 实现，考虑使用 `_mm_rsqrt_ps` SIMD 内置函数
- GLM 的做法是：`rsqrt` 计算近似值 → Newton-Raphson 迭代修正精度
- 这是一个纯算法层面的优化，不涉及库架构问题

### 2. Quaternion 运算优化（优先级：高）

**问题**：乘法慢 77-82%，旋转向量慢 92-98%，Slerp 慢 87-90%

**分析**：LTO 无帮助，说明不是内联问题，而是计算路径效率低。

**建议**：
- `Quaternion::rotate()` 方法：检查是否内部转换成了矩阵乘法。如果是，应该直接使用四元数-向量乘法公式 `v' = q * v * q^-1`，避免 4x4 矩阵构造的开销
- 四元数乘法：检查是否使用了 SIMD 优化，GLM 的四元数乘法是 SIMD 友好的
- Slerp：检查 `sin`/`cos` 调用是否可以优化为查表或近似算法

### 3. lookAt / perspective 函数优化（优先级：高）

**问题**：lookAt 慢 250-261%，perspective 慢 795-859%

**分析**：
- perspective 慢近 10 倍，非常可疑，建议重点排查
- MVP 组合在 LTO 下从慢 492% 改善到慢 190%，说明跨函数调用确实有开销，但不是主因

**建议**：
- **perspective**：检查函数内部是否有不必要的对象构造/拷贝、是否在循环中重复计算常量
- **lookAt**：检查是否有冗余的归一化调用（如果内部调用了 `normalized()`，那归一化的性能问题会放大）
- 将这两个函数标记为 `inline`，或移至头文件中

### 4. Matrix4x4 求逆优化（优先级：中）

**问题**：求逆比 GLM 慢 168-170%，LTO 完全无帮助

**分析**：README 中提到使用了"代数公式矩阵求逆（快 78%）"，但实测反而比 GLM 慢。可能的原因：
- "快 78%" 是相对于某种基准（如 cofactor expansion），而不是相对于 GLM
- 代数公式可能在通用场景下不如 GLM 使用的 cofactor expansion + SIMD 优化

**建议**：
- 检查当前实现，确认"代数公式"在通用 4x4 矩阵上是否真的更快
- 考虑针对旋转矩阵等特殊矩阵提供快速路径（旋转矩阵的逆就是转置）
- 参考 GLM 的 `inverse` 实现

### 5. 添加 LTO 支持到 CMake（优先级：中）

**建议**：
- 在 CMakeLists.txt 中添加 LTO 选项：
```cmake
option(ENABLE_LTO "Enable Link-Time Optimization" OFF)
if(ENABLE_LTO)
    include(CheckIPOSupported)
    check_ipo_supported(RESULT ipo_supported)
    if(ipo_supported)
        set(CMAKE_INTERPROCEDURAL_OPTIMIZATION TRUE)
    endif()
endif()
```
- 虽然 LTO 对当前实现帮助有限，但对于未来的优化（如内联小函数）会有好处

### 6. 编译问题修复（优先级：低）

**问题**：Linux 下编译需要手动添加 `#include <cstring>`

**建议**：
- 在 `GPU.cpp` 中添加 `#include <cstring>`
- 建议添加 CI/CD 流水线（如 GitHub Actions）自动测试多平台编译

## 总结

AstraAlgebra 在功能方面非常全面，这是它的核心竞争力。性能差距主要来自算法实现层面，建议优先排查 perspective 函数（慢近 10 倍非常异常）和 Vector3 归一化。

期待看到后续的性能改进！
```
