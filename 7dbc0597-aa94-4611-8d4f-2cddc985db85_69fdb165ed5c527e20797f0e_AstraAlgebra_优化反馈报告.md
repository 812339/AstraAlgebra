# AstraAlgebra 性能优化反馈报告

> 基于与 GLM、Eigen、cglm 的实测性能对比
> 测试环境：GCC 11.4, -O3 -mavx2, 1000万次迭代

---

## 一、总体评价

### 1.1 优势领域（值得保持）

| 测试项 | Astra | 最佳 | 差距 | 评价 |
|-------|------:|-----:|-----:|------|
| Vec3 Cross | **98.3ms** | 98.3ms | - | 🏆 全场最快 |
| Vec3 Add | 65.7ms | 64.2ms (Eigen) | +2% | ✅ 非常接近最佳 |
| Vec3 Dot | 135.5ms | 135.2ms (Eigen) | +0.2% | ✅ 几乎持平 |
| Mat4 Multiply | 299.3ms | 288.3ms (Eigen) | +4% | ✅ 竞争力强 |

**结论**：基础向量运算（加、点积、叉积）和矩阵乘法实现良好，无需改动。

### 1.2 严重性能问题（需优先修复）

| 测试项 | Astra | 最佳 | 差距 | 严重程度 |
|-------|------:|-----:|-----:|:--------:|
| **perspective** | **1200.2ms** | 125.6ms (GLM) | **-9.6x** | 🔴 极高 |
| **lookAt** | **955.1ms** | 238.4ms (GLM) | **-4x** | 🔴 极高 |
| **MVP Combine** | **2987.6ms** | 501.1ms (Eigen) | **-6x** | 🔴 极高 |
| **Quat Slerp** | **1474.2ms** | 630.7ms (Eigen) | **-2.3x** | 🟠 高 |
| **Mat4 Inverse** | **1173.1ms** | 429.0ms (GLM) | **-2.7x** | 🟠 高 |
| **Vec3 Normalize** | **239.5ms** | 85.6ms (Eigen) | **-2.8x** | 🟠 高 |
| **Quat*Vec3** | **525.6ms** | 248.6ms (Eigen) | **-2.1x** | 🟠 高 |

---

## 二、具体问题分析与优化建议

### 2.1 🔴 perspective 函数（慢 9.6 倍）

**问题**：1200ms vs GLM 125ms，差距近 10 倍，这是所有测试中最严重的问题。

**可能原因**：
1. 函数内部有严重的冗余计算
2. 可能在循环中重复计算常量
3. 可能有大量的临时对象创建/销毁
4. 三角函数调用效率低

**优化建议**：

```cpp
// 当前可能的低效实现（推测）
Matrix4x4 Matrix4x4::perspective(float fov, float aspect, float near, float far) {
    Matrix4x4 result;
    float tanHalfFov = tan(fov / 2.0f);  // 每次调用都计算
    // ... 大量赋值操作
    return result;
}

// 参考 GLM 的高效实现思路
Matrix4x4 Matrix4x4::perspective(float fov, float aspect, float near, float far) {
    Matrix4x4 result = Matrix4x4::zero;  // 零矩阵，避免构造开销
    float f = 1.0f / tanf(fov * 0.5f);   // 使用 tanf 而非 tan，使用乘法而非除法
    
    result.m[0][0] = f / aspect;
    result.m[1][1] = f;
    result.m[2][2] = (far + near) / (near - far);
    result.m[2][3] = (2.0f * far * near) / (near - far);
    result.m[3][2] = -1.0f;
    
    return result;
}
```

**关键优化点**：
1. 使用 `tanf` 而非 `tan`（float 版本更快）
2. 预计算 `1.0f / tan(...)` 而不是多次除法
3. 直接赋值矩阵元素，避免函数调用
4. 考虑将函数标记为 `inline`

---

### 2.2 🔴 lookAt 函数（慢 4 倍）

**问题**：955ms vs GLM 238ms，场景函数性能严重不足。

**可能原因**：
1. 内部调用了慢速的 `normalized()` 函数
2. 向量运算没有内联
3. 可能有动态内存分配

**优化建议**：

```cpp
// 关键：避免调用慢速的 normalized()
// 如果内部实现是：
Vector3 zaxis = (eye - target).normalized();  // 这是瓶颈！

// 改为手动归一化，使用 rsqrt 快速平方根倒数
// 或确保 normalize 使用 SIMD 优化

// 参考 GLM 的实现：
Matrix4x4 Matrix4x4::lookAt(const Vector3& eye, const Vector3& target, const Vector3& up) {
    Vector3 f = (target - eye).normalized();  // 前向向量
    Vector3 s = f.cross(up).normalized();     // 右向向量 - 这里慢！
    Vector3 u = s.cross(f);                    // 上向向量
    
    Matrix4x4 result = Matrix4x4::identity;
    result.m[0][0] = s.x; result.m[0][1] = s.y; result.m[0][2] = s.z;
    result.m[1][0] = u.x; result.m[1][1] = u.y; result.m[1][2] = u.z;
    result.m[2][0] = -f.x; result.m[2][1] = -f.y; result.m[2][2] = -f.z;
    result.m[0][3] = -s.dot(eye);
    result.m[1][3] = -u.dot(eye);
    result.m[2][3] = f.dot(eye);
    return result;
}
```

**关键优化点**：
1. **优先优化 Vec3::normalized()**（见 2.5），lookAt 的性能直接取决于它
2. 使用栈上分配，避免堆内存
3. 直接构造矩阵，避免多次赋值

---

### 2.3 🟠 Vec3 Normalize（慢 2.8 倍）

**问题**：239.5ms vs Eigen 85.6ms，基础运算严重落后。

**根本原因**：没有使用 SIMD `rsqrt`（快速平方根倒数）指令。

**优化建议**：

```cpp
// 当前可能的低效实现（推测）
Vector3 Vector3::normalized() const {
    float len = sqrt(x*x + y*y + z*z);  // 慢速 sqrt
    return Vector3(x/len, y/len, z/len);
}

// 参考 Eigen 的高效实现（使用 rsqrt）
Vector3 Vector3::normalized() const {
    // 使用 AVX 的 rsqrt 指令
    #if defined(__AVX__)
        __m128 v = _mm_set_ps(0, z, y, x);
        __m128 dp = _mm_dp_ps(v, v, 0x7F);  // 点积 x*x + y*y + z*z
        __m128 rlen = _mm_rsqrt_ss(dp);      // 快速平方根倒数
        float s = _mm_cvtss_f32(rlen);
        return Vector3(x*s, y*s, z*s);
    #else
        // 回退到标准实现，但使用更快的算法
        float rlen = 1.0f / std::sqrt(x*x + y*y + z*z);
        return Vector3(x*rlen, y*rlen, z*rlen);
    #endif
}
```

**关键优化点**：
1. 使用 `_mm_rsqrt_ss`（SSE）或 `_mm256_rsqrt_ps`（AVX）快速计算 1/sqrt(x)
2. 用乘法 `*rlen` 替代除法 `/len`
3. Eigen 的 `normalized()` 是模板内联的，确保 AstraAlgebra 也标记为 `inline`

---

### 2.4 🟠 Mat4 Inverse（慢 2.7 倍）

**问题**：1173ms vs GLM 429ms，矩阵求逆算法效率低。

**README 声称**："代数公式矩阵求逆（快 78%）"

**实测结果**：比 GLM 慢 170%

**分析**：
- "快 78%" 可能是相对于某种未优化的基准（如伴随矩阵法）
- 但对于旋转矩阵，GLM 可能使用了快速路径（旋转矩阵的逆 = 转置）

**优化建议**：

```cpp
// 为旋转矩阵提供快速路径
Matrix4x4 Matrix4x4::inverse() const {
    // 检测是否为纯旋转矩阵（最后一行是 0,0,0,1）
    if (m[3][0] == 0 && m[3][1] == 0 && m[3][2] == 0 && m[3][3] == 1) {
        // 旋转矩阵的逆 = 转置
        Matrix4x4 result;
        // 转置 3x3 部分
        for (int i = 0; i < 3; ++i)
            for (int j = 0; j < 3; ++j)
                result.m[i][j] = m[j][i];
        // 平移部分取反并旋转
        result.m[0][3] = -(m[0][0]*m[0][3] + m[1][0]*m[1][3] + m[2][0]*m[2][3]);
        result.m[1][3] = -(m[0][1]*m[0][3] + m[1][1]*m[1][3] + m[2][1]*m[2][3]);
        result.m[2][3] = -(m[0][2]*m[0][3] + m[1][2]*m[1][3] + m[2][2]*m[2][3]);
        result.m[3][3] = 1.0f;
        return result;
    }
    // 通用矩阵使用当前算法
    return inverseGeneric();
}
```

---

### 2.5 🟠 Quaternion 运算（慢 2-2.3 倍）

**问题**：
- Quat Multiply: 600ms vs 343ms (GLM)
- Quat*Vec3: 525ms vs 248ms (Eigen)
- Quat Slerp: 1474ms vs 630ms (Eigen)

**优化建议**：

```cpp
// Quat * Vec3 优化 - 避免转换为矩阵
// 当前可能的低效实现：
Vector3 Quaternion::rotate(const Vector3& v) const {
    Matrix4x4 m = toMatrix();  // 构造 4x4 矩阵 - 昂贵！
    return m * v;
}

// 高效实现 - 直接使用四元数乘法公式
Vector3 Quaternion::rotate(const Vector3& v) const {
    // v' = q * v * q^-1
    // 对于单位四元数，q^-1 = conjugate(q)
    Vector3 qv(x, y, z);
    Vector3 t = qv.cross(v) * 2.0f;
    return v + t * w + qv.cross(t);
}
```

**Slerp 优化**：
- 检查是否使用了 `sin`/`acos` 的查表优化
- Eigen 的 Slerp 使用了更快的近似算法

---

## 三、各库实现参考

### 3.1 GLM 的优势实现

GLM 在以下函数上表现最佳，建议参考其实现：

| 函数 | GLM 性能 | 关键优化 |
|-----|---------|---------|
| perspective | 125.6ms | 最小化计算，避免冗余 |
| lookAt | 238.4ms | 高效的向量运算链 |
| Quat Multiply | 343.7ms | SIMD 友好的四元数乘法 |

**GLM 源码位置**：https://github.com/g-truc/glm

### 3.2 Eigen 的优势实现

Eigen 在以下函数上表现最佳：

| 函数 | Eigen 性能 | 关键优化 |
|-----|-----------|---------|
| Vec3 Normalize | 85.6ms | `rsqrt` SIMD 指令 |
| Quat*Vec3 | 248.6ms | 避免矩阵转换的旋转公式 |
| Quat Slerp | 630.7ms | 快速三角函数近似 |

**Eigen 源码位置**：https://gitlab.com/libeigen/eigen

---

## 四、优先级排序

### P0 - 立即修复（影响最大）

1. **perspective 函数** - 慢 9.6 倍，最常用函数之一
2. **Vec3::normalized()** - 慢 2.8 倍，影响 lookAt 等场景函数

### P1 - 高优先级

3. **lookAt 函数** - 慢 4 倍，受益于 normalized() 优化
4. **Mat4::inverse()** - 慢 2.7 倍，添加旋转矩阵快速路径
5. **Quat::rotate()** - 慢 2.1 倍，避免矩阵转换

### P2 - 中优先级

6. **Quat::slerp()** - 慢 2.3 倍，优化三角函数计算
7. **MVP Combine** - 慢 6 倍，受益于上述所有优化

---

## 五、测试复现

如需复现测试结果，使用以下代码：

```bash
# 编译命令
g++ -O3 -mavx2 -std=c++17 \
  -I./AstraAlgebra/include \
  -I./glm \
  -I./eigen \
  -I./cglm/include \
  four_lib_benchmark.cpp cglm_bench.cpp \
  -L./AstraAlgebra/build -lAstraAlgebra \
  -o four_lib_benchmark

./four_lib_benchmark
```

完整测试代码已提供，可验证所有数据。

---

## 六、总结

AstraAlgebra 在**功能完整性**方面具有独特优势（碰撞检测、Transform 系统、GPU 加速），但在**基础数学运算性能**方面与成熟库还有显著差距。

**核心建议**：
1. 优先修复 `perspective` 和 `normalized()`，这两个函数影响最大
2. 参考 GLM/Eigen 的实现，特别是 SIMD 优化
3. 考虑添加旋转矩阵等特殊情况的快速路径
4. 建议添加 CI 性能回归测试，防止性能退化

期待看到 AstraAlgebra 的性能改进！

---

*报告生成时间：2025年*
*测试环境：GCC 11.4, Ubuntu 22.04, AVX2*
